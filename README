LocalCountry
=================================

Enhance country code fields with answers for unroutable addresses.
Loads a a file mapping CIDR blocks (presumed to be RFC 1918 subnets) to country codes.
